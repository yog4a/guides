# =====================================================================
# Ensure SSH (port 20220) is never affected by UFW jails
# =====================================================================

[ufw-portScan]
ignorecommand = iptables -C INPUT -p tcp --dport 20220 -j ACCEPT 2>/dev/null || iptables -I INPUT -p tcp --dport 20220 -j ACCEPT

[ufw-synFlood]
ignorecommand = iptables -C INPUT -p tcp --dport 20220 -j ACCEPT 2>/dev/null || iptables -I INPUT -p tcp --dport 20220 -j ACCEPT

[ufw-udpFlood]
ignorecommand = iptables -C INPUT -p tcp --dport 20220 -j ACCEPT 2>/dev/null || iptables -I INPUT -p tcp --dport 20220 -j ACCEPT

[ufw-wrongPort]
ignorecommand = iptables -C INPUT -p tcp --dport 20220 -j ACCEPT 2>/dev/null || iptables -I INPUT -p tcp --dport 20220 -j ACCEPT